function Search(props){
    const {value, onValueChange} = props
    return(
        {/* <div className='app-search'>
        <input className='search' type='text' placeholder='Search' value={value} onChange={(event) => {onValueChange(event.target.value)}}/>
      </div> */}
    )
}

export default Search;